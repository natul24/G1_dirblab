"""Model and detector modules for event prediction.

This package contains code that turns prepared feature tables into predictions.
It currently includes the Step 4 deterministic rule-based detector and the
binary logistic regression pass classifier. Trained artifacts are written under
`artifacts/models/`, not inside this source package.
"""
