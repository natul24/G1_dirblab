"""Evaluation helpers for rule-based and learned event detectors.

This package currently exposes classification metric functions used by Step 4
rule-based detection and the binary pass classifier. Evaluation code is kept
separate from model code so metrics can be reused consistently across stages.
"""
