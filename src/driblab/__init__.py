"""Source package for the Driblab event-detection project.

The package is organized by pipeline responsibility: `etl` builds and validates
data tables, `features` creates modeling features and match splits, `models`
contains rule-based and learned detectors, `evaluation` contains metrics, and
`validation` contains shared input checks.
"""
