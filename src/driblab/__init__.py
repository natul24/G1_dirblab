"""Source package for the Driblab event-detection project.

The package is organized by pipeline responsibility: `etl` builds and validates
data tables, `features` contains match split helpers, and `validation` contains
shared input checks.
"""
